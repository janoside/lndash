div(class="table-responsive")
	table(class=("table " + (channelTableNotBordered ? "" : "table-bordered")))
		thead
			tr
				th ID
				th Node 1
				th Node 2
				th Last Update
				th(class="text-right") Capacity
		
		tbody
			each channel, index in channels
				include ./channel-table-row.pug