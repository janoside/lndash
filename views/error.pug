extends layout4

block breadcrumb
	li.breadcrumb-item
		a(href='/') Home
	li.breadcrumb-item Error

block content
	h1 Error
	hr

	if (message)
		p !{message}
	else
		p Unknown error

	if (error)
		h2 #{error.status}
		pre
			code(class="json") #{error.stack}
